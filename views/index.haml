- if @interviews.length > 0
  %ul
    - @interviews.each do |interview|
      %li
        %p.date= interview.created_at.strftime("%d.%m.%Y")
        %a{:href => "/#{interview.slug}/", :title => "View an interview with #{interview.person}."}
          %span= interview.person
